import React from 'react';
import ReactDOM from 'react-dom';
import EmojiCodeApp from './EmojiCodeApp';
import './index.css'; // You may need to import global styles here

ReactDOM.render(
  <React.StrictMode>
    <EmojiCodeApp />
  </React.StrictMode>,
  document.getElementById('root')
);
